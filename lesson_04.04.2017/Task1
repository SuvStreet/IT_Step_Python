# Напишите программу, запрашивающую у пользователя ввод числа.
# Если число принадлежит диапазону от -100 до 100, то создается объект одного класса,
# во всех остальных случаях создается объект другого класса.
# В обоих классах должен быть метод-конструктор __init__, который в первом классе возводит число в квадрат,
# а во-втором - умножает на два.

class One():
    def __init__(self, n):
        n = n ** 2
        print(n)

class Two():
    def __init__(self, n):
        n *= 2
        print(n)

# main
n = int(input('Введите число: '))

if -100 < n < 100:
    obj = One(n)
else:
    obj = Two(n)

input('Нажмите Enter, чтобы выйти ...')
